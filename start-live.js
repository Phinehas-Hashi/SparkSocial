import{auth,db}from './firebase.js';import{onAuthStateChanged}from 'https://www.gstatic.com/firebasejs/12.15.0/firebase-auth.js';import{addDoc,collection,serverTimestamp}from 'https://www.gstatic.com/firebasejs/12.15.0/firebase-firestore.js';const $=s=>document.querySelector(s);onAuthStateChanged(auth,async u=>{if(!u){location.replace('login.html');return}await u.reload();if(!u.emailVerified){location.replace('verify-email.html');return}$('#liveForm').onsubmit=async e=>{e.preventDefault();const title=$('#title').value.trim();if(!title)return;$('#start').disabled=true;try{const ref=await addDoc(collection(db,'liveSessions'),{hostId:u.uid,title,status:'live',startedAt:serverTimestamp(),endedAt:null,viewerCount:0,peakViewerCount:0,likesCount:0,chatEnabled:true,createdAt:serverTimestamp()});location.replace(`live.html?id=${ref.id}`)}catch(err){$('#status').className='error';$('#status').textContent=err.message;$('#start').disabled=false}}});